plugins {
    id 'java'
}

// Si el nombre de tu proyecto es 247ClubManagementSystem, entonces el nombre del proyecto debe coincidir aquí
// Si es diferente, asegúrate de reemplazar '247ClubManagementSystem' con el nombre correcto
group '247ClubManagementSystem'
version '1.0-SNAPSHOT'

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

test {
    useJUnit()
}
